{% extends 'base.html.twig' %}

{% block title %}Home Page{% endblock %}

{# {% block head %}
    {{ parent() }}
{% endblock %} #}

{# {% set route_name = app.request.attributes.get('_route') %} #}

{# {% block navigation %}
    
{% endblock %} #}

{% block content %}
    <!-- Content Wrapper. Contains page content -->
    <div class="content-wrapper">
      <section class="content-header">
        <h1>ivd24 Support | Home Page</h1>

        {% if is_granted("ROLE_SUPPORT") %} 
          <div>
            <p>Hi {{ app.user.userid }} You are logged in as Supporter!</p>
            {# <p>Your email={{ app.user.email }} </p> #}
          </div>
        {% endif %}

        {% if is_granted("ROLE_ADMIN") %} 
          <div>
            <p>Hi {{ app.user.username }} You are logged in as Admin!</p>
            {# <p>Your email={{ app.user.email }} </p> #}
          </div>
        {% endif %}

        

      </section>
      <!-- Main content -->
     
      
			
      <!-- /.Main content -->
    </div>
  <!-- /.content-wrapper -->
{% endblock %}

{# {% block footer %}
    {{ parent() }}
{% endblock %} #}