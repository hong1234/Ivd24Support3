{% extends 'base.html.twig' %}

{% block title %}StatisticUser Delete{% endblock %}

{# {% block head %}
    {{ parent() }}
{% endblock %} #}

{# {% block navigation %}
    
{% endblock %} #}

{% block content %}
    <!-- Content Wrapper. Contains page content -->
    <div class="content-wrapper">
      <section class="content-header">
        <h1>StatisticUser löschen</h1>
      </section>
      <!-- Main content -->
      <section class="content">
        <div class="row">
          <!-- left column -->
          <div class="col-md-6">

          {# <form action="/statisticuser/{{user_id}}/delete" method="POST"> #}
          <form action="{{ path('statisticuser_delete', {'uid': user_id}) }}" method="POST">
            <div class="box box-warning">
                {# <div class="box-header with-border">
                    <h3 class="box-title">Statistic User</h3>
                </div> #}
                <!-- /.box-header -->
                <div class="box-body">
                    <!-- text input -->
                    <div class="form-group">
                        <label for="Email">Email-Adresse</label>
                        <input type="email" class="form-control" id="email" name="email" placeholder="E-Mailadresse" value="{{email}}" readonly>
                    </div>
                    <div class="form-group">
                        <label for="username">Username</label>
                        <input type="username" class="form-control" id="username" name="username" placeholder="Username" value="{{username}}" readonly>
                    </div>
                    
                    <div class="form-group">
                        <label for="Geschaefststelle">Geschäftsstelle</label>
                        <input type="geschaeftsstelle" class="form-control" id="geschaeftsstelle" name="geschaeftsstelle" placeholder="geschaeftsstelle" value="{{geschaeftsstelle}}" readonly>
                    </div>
                </div>
			    <div class="box-footer">
                    <label for="Loeschen">Wollen Sie diesen Datensatz wirklich löschen?</label><br>
                    <button type="submit" class="btn btn-primary" name="123button" value="true">NEIN</button>
                    <button type="submit" class="btn btn-danger" name="savebutton" value="true">- JA -</button>
                </div>
                <!-- /.box-body -->
            </div>
          </div>
        </div>
      </section>
    </div>
  </form>
  <!-- /.content-wrapper -->
{% endblock %}

{# {% block footer %}
    {{ parent() }}
{% endblock %} #}