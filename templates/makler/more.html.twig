<div class="col-md-6">
    <div class="box box-warning">
    <div class="box-body">
        <b>Adresse des Maklers (User-Id: {{user_id}})</b><br><br>

        {{makler.strasse}}<br>
        {{makler.plz}} {{makler.ort}}<br>
        <br>
        Telefon: {{makler.telefon}}<br>
        Mobil: {{makler.mobil}}<br>
        <br>
        <br>
        <br>
        <br>
        <br>
    </div>
    </div>
</div>

<div class="col-md-6">
    <div class="box box-warning">
        <div class="box-body">
            <b>Weitere Infos über Makler (User-Id: {{user_id}})</b><br><br>
            {% if BcClub == 'Ja' %}
            <a href='/support/bcuser/{{user_id}}/edit' target='_blank'>BusinessClub:</a> Ja<br>
        {% else %}
            BusinessClub: Nein<br>
        {% endif %}

        {% if stock.ak_anzahl > 0 %}
           <a href='/stock/shareholders/{{user_id}}/edit' target='_blank'>Aktien:</a> Ja ({{stock.ak_anzahl}})<br>
        {% else %}
            Aktien: Nein<br>
        {% endif %}

        {% if ftp.ftp_pause == 'N' %}
            FTP-Pause: Nein <br> 
            <a href='/support/server/{{user_id}}/edit' target='_blank'>FTP-Import pausieren</a><br>
        {% else %}
            FTP-Pause: Ja <br>  
            <a href='/support/server/{{user_id}}/starten' target='_blank'>FTP-Import starten</a><br>
        {% endif %}
        <br>

        Statistik der letzten 4 Wochen: <br>

        Anzahl Anfragen: {{request.request_az}}<br>
        Anzahl Exposeaufrufe: {{expose.expose_az}}<br>
        Anzahl Immobilien aktuell: {{object.objekt_az}}<br>
        <a href='https://anbieter.ivd24immobilien.de/{{makler.seo_url}}' target='_blank'>Immobilien online</a><br>
        </div>
    </div>
</div>


{# <table cellpadding="5" cellspacing="0" border="0" style="padding-left:50px;">
<tbody>
<tr><td>Adresse:</td><td>{{makler.plz}} {{makler.ort}}</td></tr>
<tr><td>Telefon:</td><td>{{makler.telefon}}</td></tr>
<tr><td>BusinessClub:</td><td>{{BusinessClub}}</td></tr>
{% if BusinessClub == 'Ja' %}
    <tr><td>BusinessClub:</td><td><a href='/support/bcuser/{{user_id}}/edit' target='_blank'>BcUser Detail</a></td></tr>  
{% endif %}
</tbody>
</table> #}